/**
 * Home Messages.
 *
 */

import {defineMessages} from "react-intl";


/**
 * Return a translation object with all messages for the container.
 */
export default defineMessages(
    {
        header: {
            id: "{{ cookiecutter.module_name }}.container.Home.header",
            defaultMessage: "This is HomePage component!",
        },
    }
);
